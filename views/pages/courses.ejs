<html>
    <head>

    </head>
    <body>
        <%- include ("../partials/header.ejs") %>
        <section>
            <ul>
                <% for(var i = 0; i < courses.length; i++) {  %>
                    <li>
                        <h3> <%= courses[i].title %> </h3>
                        <p> <%= courses[i].description %> </p>
                        <form action="/addCourse/<%= courses[i].id %>" method="POST">
                            <button type="submit">Enroll</button>
                        </form>
                    </li>
                  <% } %>
            </ul>
        </section>
    </body>
</html>